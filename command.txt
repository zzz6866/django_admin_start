pip freeze > requirements.txt # 인터프리터 리스트 설치
pip install -r requirements.txt # 인터프리터 생성 (로컬에서 만든 리스트)
python manage.py celery myweb worker -l info # celery 실행
python manage.py makemigrations
python manage.py migrate
python manage.py migrate kombu_transport_django

[program:webserver]
command={{ PYTHON }} {{ PROJECT_DIR }}/manage.py runserver --noreload

[program:celeryworker]
command={{ PYTHON }} {{ PROJECT_DIR }}/manage.py celery -A proj worker -l info

[program:celerybeat]
command={{ PYTHON }} {{ PROJECT_DIR }}/manage.py celery beat -l info

[program:webserver]
{% if settings.DEBUG %}
command={{ PYTHON }} {{ PROJECT_DIR }}/manage.py runserver
{% else %}
command={{ PYTHON }} {{ PROJECT_DIR }}/manage.py runfcgi host=127.0.0.1 port=8025
{% endif %}